#ifndef PRIME_H
#define PRIME_H

/*The size of the hash table should be a prime number. 
So, if the the hash table is resized its size new should be also a prime number. 
This module allows to find a prime number*/

bool isPrime(const int x);

int nextPrime(int x);

#endif  //PRIME_H


